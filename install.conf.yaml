- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.zshrc: zsh/zshrc
    ~/.tmux.conf: tmux.conf
    ~/.vimrc: vimrc
    ~/.zgen: zsh/zgen

- create:
    - ~/.vim/undo-history
    - ~/.vim/backups
    - ~/.vim/swaps

- shell:
    - [git submodule update --init --recursive, Installing submodules]
